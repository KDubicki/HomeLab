# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# Ansible
*.retry
inventory.ini

# System & OS
.DS_Store
*.log
tmp/

# AI — track shared Claude config (skills, commands), ignore machine-local settings
.claude/settings.local.json

# Secrets — NEVER commit (values live in Vault or a gitignored bootstrap only)
*.auto.tfvars
*.env
.env
*.pem
*.key
*.p12
*.pfx
id_ed25519*
id_rsa*
*.kubeconfig
kubeconfig
vault-init*.json
*unseal*
.vault-password
*.age
age-key*
